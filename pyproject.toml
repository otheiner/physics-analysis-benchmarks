[project]
name = "parametr-bench"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "statsmodels",
    "matplotlib",
    "litellm",
    "python-dotenv",
    "requests",
    "astropy",
    "docker"
]
